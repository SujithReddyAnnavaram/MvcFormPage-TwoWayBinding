# MvcFormPage-TwoWayBinding
Spring MVC web application demonstrating two-way data binding between form inputs and backend model objects
This project is a simple Spring MVC web application that showcases two-way data binding between the UI form and backend model using Spring’s form handling capabilities.

It demonstrates how user input from a web form is automatically bound to a Java object and how updated data is reflected back in the view.
